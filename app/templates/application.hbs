{{#if signedIn}}
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
<div class="container-fluid">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="dashboard/">
      TitanFinance
    </a>
  </div>
  <div id="navbar" class="navbar-collapse collapse">
    <ul class="nav navbar-nav navbar-right">
      {{#link-to 'dashboard' tagName="li" href=false}}
        {{#link-to 'dashboard' bubbles=false}}
        <span class="glyphicon glyphicon-home"></span>
        {{/link-to}}
      {{/link-to}}
      {{#link-to 'search' tagName="li" href=false}}
        {{#link-to 'search' bubbles=false}}
        <span class="glyphicon glyphicon-search"></span>
        {{/link-to}}
      {{/link-to}}
      <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><img {{bind-attr src=gravatarUrl}}></a>
          <ul class="dropdown-menu" role="menu">
            {{#link-to 'profile' tagName="li" href=false}}
              {{#link-to 'profile' bubbles=false}}
                Profile
              {{/link-to}}
            {{/link-to}}
            {{#link-to 'settings' tagName="li" href=false}}
              {{#link-to 'settings' bubbles=false}}
                Profile Settings
              {{/link-to}}
            {{/link-to}}
            <li class="divider"></li>
            <li class="dropdown-danger"><a href="#" {{action 'logout'}}>Log out</a></li>
          </ul>
        </li>
    </ul>
  </div>
</div>
</nav>
{{/if}}
{{outlet}}
