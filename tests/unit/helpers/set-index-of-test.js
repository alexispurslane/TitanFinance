import { setIndexOf } from 'titan-finance/helpers/set-index-of';

module('SetIndexOfHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = setIndexOf(42);
  ok(result);
});
